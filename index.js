/*function persona (nombre, edad) {
    if (edad >= 18) {
        return `Puedes ingresar  ${nombre},`;
    } else {
        return `lo siento ${nombre} no puedes ingresar`;
    }
}

console.log(persona("Ricardo", 22));*/

//función de antrito 

/*function auto (modelo, año) {
    if (año >= 2020) {
        return `El auto ${año} es aceptable`;
    } else {
        return `El auto ${modelo} no es aceptable`;
    }
}

console.log(auto("Subaru outback", 1999));*/


//Creando un objeto

/*let dildo = {
    modelo: "dildo con ventosa",
    color: "cafe",
    tamaño: "mediano"
};*/




//console.group(), y console.groupEnd()
/*console.group("modelos de mercedes benz");
console.log(" Mercedes cls");
console.log(" Mercedes gle");
console.log(" Mercedes glc");

console.groupEnd();*/


//console.assert(), solo sirve para avisar por consola con un mensaje, de forma sutil
console.assert(20 < 100);
console.assert(20 > 100); // Asertion failed

















